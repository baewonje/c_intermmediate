#include <stdio.h>

void main(){
		int world=7;
		int number=1;
		printf("word sector: %d", world);
		while(number<=50){				 // while([상수/변수/표현식]){        {}=>statement block
				printf("wollo guest%d\n", number); //	--------------- ; 명령문, 명령어 라인 => statement
				number++;				 // {
										 // step1] while ()안에 있는 상수,변수 또는 표현식을 평가한다.
										 // step2] 병가한 결과가 참(1)이면 while 이하의 statement block
}										 //			을 수행한다. 거짓(0)이면 statement block을 빠져나온다.
										 // step3] step2에서 참인경우 step1로 이동한다.
/*      [{( : opening    
 *      ]}) : closing   
 * 소괄호 : () : round bracket
 *
 * 중괄호 : {} : brace
 *
 * 대괄호 : [] : sguare bracket

 */
}										
